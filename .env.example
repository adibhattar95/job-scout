# Optional configuration. Load explicitly with: set -a; source .env; set +a
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.1:latest
SCOUT_DATA_DIR=data/local
# JSearch integration is a later milestone. Do not commit real keys.
# JSEARCH_API_KEY=
